---
scope: Whole AI-RAG product: consistency contract for independently-built slices from identity-and-tenancy through production-hardening
purpose: build-substrate
altitude: feature
updated: 2026-09-21T13:04
---

- (event) Run workspace created; coaching path selected; no parent spine; no SPEC.md; sources are AGENTS.md, docs/, compose.yaml, apps/api, apps/web, .blueprint-origin.yaml (ai-rag-fastapi-react 1.0).
- (decision by user) Working mode: coaching path. Fast path declined.
- (assumption) Deliverable purpose defaulted to build-substrate only (spine for implementers of next slices). User did not name a second audience; will override if they do.
- (constraint) Tenant isolation is a security boundary; tenant_id comes from authenticated context, never from an untrusted client field used as the only filter. (AGENTS.md, docs/architecture.md)
- (constraint) Domain logic stays independent of FastAPI, SQLAlchemy, Celery, Qdrant, and LangChain. Use cases do not take Request/Response. HTTP adapters map domain errors to status codes.
- (constraint) Layout is binding: apps/api/src/app/{domain,application,infrastructure,bootstrap}; apps/web/src/features + apps/web/src/api. Slice order: identity-and-tenancy → documents → ingestion → retrieval → chat → rag-engineering → evaluation → production-hardening.
- (constraint) Domain model already present: Tenant→User→Conversation→Message; Tenant→Document; Document lifecycle PENDING→PROCESSING→READY|FAILED. RAG payload must include tenant_id, document_id, filename, page, chunk_index.
- (direction) Brownfield spine: ratify conventions the code and docs already show; do not invent a competing architecture. Open questions are only where two later slices could still diverge.
- (decision by user) Paradigm: hexagonal ports-and-adapters [ADOPTED]. Binds all API slices and the web's relationship to the API. Prevents layered 'service' leakage of FastAPI/SQLAlchemy/Celery/Qdrant/LangChain into domain/application, and prevents per-feature mini-apps with incompatible tenant/vector contracts. Mapping: domain=apps/api/src/app/domain; application=.../application; adapters=.../infrastructure; composition=.../bootstrap; UI features=apps/web/src/features (not domain).
- (version) Web-checked 2026-09-18: FastAPI current 0.141.1 (repo 0.115.12); Qdrant current v1.19.1 (compose v1.13.2); SQLAlchemy stable 2.0.52 (2.1.0rc1 exists, not GA); LangChain 1.3.15 / langchain-openai 1.4.1 (openai SDK >=2.45). React 19.1.0 and Celery 5.4.0 remain the installed seed.
- (constraint) Installed seed stack (not yet user-locked): Python >=3.12, FastAPI 0.115.12, uvicorn 0.34.2, pydantic 2.11.7, celery 5.4.0, redis 5.2.1, httpx 0.28.1; React 19.1.0 + Vite 6.3.5 + TS 5.8.3 + RRD 7.6.1; compose postgres:16-alpine, redis:7.4-alpine, qdrant v1.13.2. SQLAlchemy, qdrant-client, openai, LangChain are named in docs/AGENTS.md but not in lockfiles.
- (decision by user) RAG/LLM stack: thin owned adapters in infrastructure — qdrant-client, official openai SDK, own extract/chunk — each behind a domain port. LangChain is not the RAG engine. Binds ingestion, retrieval, chat, rag-engineering. Prevents a fat LCEL/'RagEngine' object and prevents chat vs ingestion growing different chain styles. Pipeline orchestration lives in application use cases, not in the adapter.
- (decision) Postgres adapter: SQLAlchemy 2.x (stable 2.0.52 at authoring) + Alembic in infrastructure only [ADOPTED from AGENTS.md forbidding domain SQLAlchemy + user did not contradict]. Binds identity, documents, chat persistence. Prevents SQLModel vs raw psycopg vs per-slice ORMs. Versions remain seed; code owns pins once added.
- (direction) Installed lockfile versions stay seed (FastAPI 0.115.12, Qdrant v1.13.2, etc.); version bumps are not architecture invariants. AGENTS.md still names LangChain as a forbidden domain import — still true, but the engine choice has moved; offer to update that source at finalize.
- (decision by user) Celery worker is an adapter, not a second architecture [ADOPTED]. HTTP and worker share the composition root and invoke the same application use cases. Job payload carries the enqueuing actor (tenant/user from authenticated context at enqueue time), never a client-supplied tenant_id as the only filter. Domain and application never import Celery. Binds ingestion and any later async jobs (evaluation, reindex). Prevents a Celery-owned ingest pipeline that bypasses ports/use cases, and prevents worker vs HTTP drifting on tenant filters and document status transitions.
- (decision by user) Authenticated context is an explicit domain Actor value object (tenant_id + user_id; roles deferred until a slice needs them). HTTP adapter builds Actor from the verified token; worker adapter builds Actor from the job record (enqueuing actor). Every use case that touches a tenant resource takes Actor as an argument and uses actor.tenant_id for isolation; never Request, never contextvars, never a client field as the only tenant filter. Binds all slices. Prevents implicit-scope tenant leaks in workers/tests and prevents per-slice primitive tenant_id/user_id signatures that drift.
- (decision by user) Document bytes live in object storage behind a domain port (DocumentBytes/BlobStore). Infrastructure adapter speaks the S3 API so MinIO, Amazon S3, or another S3-compatible store can be swapped via endpoint/credentials env — not via a MinIO-specific SDK in domain/application. Compose seed intended: MinIO. Object keys must include tenant_id (and document_id) so listing/get cannot cross tenants. Binds documents + ingestion. Prevents API-local disk the worker cannot see, and prevents a MinIO-only client leaking into use cases.
- (decision) Qdrant: one shared collection; every point payload includes tenant_id, document_id, filename, page, chunk_index; every query MUST filter by actor.tenant_id [ADOPTED from docs/rag.md; user did not contradict]. Collection-per-tenant is out. Binds ingestion + retrieval + chat. Prevents two slices inventing different isolation schemes and unfiltered searches.
- (version) MinIO Community Edition is source-only as of late 2025; GitHub repo archived read-only Apr 2026; official prebuilt Docker releases no longer maintained (last Docker Hub RELEASE around 2025-10-15). Garage (Deuxfleurs) is a maintained S3-compatible compose alternative; docker dxflrs/garage, current cited v2.4.1 / v2.2.0 in 2026 docs — confirm exact tag if adopted.
- (question) Compose object-store seed: still MinIO (build from source or pin a legacy image) vs switch local seed to Garage while keeping the S3 adapter.
- (version) Garage current stable docker tag dxflrs/garage:v2.4.1 (released 2026-09-08).
- (decision by user) Compose object-store seed is Garage (dxflrs/garage:v2.4.1), not MinIO. Application still uses an S3-compatible adapter (endpoint, region, path-style, credentials from env) so prod can point at Amazon S3 or another S3 API without a new port. Binds local ops + documents/ingestion. Prevents pinning an unmaintained MinIO image and prevents a Garage-specific client in domain/application.
- (decision) One object bucket for document bytes; isolation is key prefix {tenant_id}/{document_id}/... not bucket-per-tenant [consequence of tenant-prefixed keys + one Qdrant collection]. Prevents documents vs ingestion splitting bucket naming and Garage ACL layout.
- (decision by user) Browser auth is Bearer JWT [ADOPTED]. Login: API issues a JWT signed with jwt_secret from env. HTTP adapter verifies the token and builds Actor from signed claims (user_id + tenant_id) — those claims are authenticated context, not an untrusted client filter. Web sends Authorization: Bearer. Token is not forwarded to the worker; jobs already carry Actor. Cookies/sessions/OIDC deferred. Web convention: keep token in memory, not localStorage. Binds identity + every HTTP feature. Prevents cookie/CSRF vs bearer split across documents/chat and prevents tenant_id taken from JSON body as the only filter.
- (decision) JWT issuer is this API over local User records (email + tenant_id already in domain). External IdP/OIDC deferred to production-hardening. Prevents identity slice assuming local users while a later slice assumes Auth0.
- (decision by user) Application use cases and domain ports are sync [ADOPTED]. Celery worker calls execute() directly. FastAPI routes for product use cases are sync def. SQLAlchemy sync Session in infrastructure. Readiness probes may stay async at the HTTP adapter edge only — /ready is not the template for ProcessDocument/AskQuestion. Binds all product slices + worker. Prevents async use cases that Celery cannot call without a second style, and prevents HTTP-async vs worker-sync duplicate ports.
- (decision by user) Remaining pack accepted: shared Postgres schema with tenant_id on every tenant-owned row; repositories always filter by actor.tenant_id; RLS deferred to production-hardening. IDs are UUID strings created in application, not serials. Domain errors mapped by HTTP adapter (TenantIsolationError 403, DomainError 409, missing 404); envelope is FastAPI detail string as existing handlers. Redis+Celery is the only async fabric; no extra event bus. Compose is the named environment (Postgres, Redis, Qdrant, Garage, api, worker, web); cloud/k8s/prod topology deferred to production-hardening. Page-aware chunks stand; hybrid/rerank/query-rewrite deferred to rag-engineering; model names in env.
- (version) Intended adds web-checked 2026-09-18: SQLAlchemy 2.0.52, Alembic 1.19.2, qdrant-client 1.13.2 to match compose Qdrant v1.13.2 (not client 1.19.0), openai 3.13.0, boto3 1.43.79, PyJWT 2.13.0. FastAPI remains 0.115.12 and Qdrant server v1.13.2 as installed seed; bumps are not invariants.
- (event) Coaching decisions complete; distilling architecture spine.
- (event) Reviewer gate: lint_spine 0 findings. Applied autofixes: Garage labeled intended Compose add; GetReadiness grandfathered; NotFoundError mapping; Qdrant collection chunks + text payload + deterministic point ids + one embed env; DocumentBytes key tenant_id/document_id/filename; ProcessDocument owns mark_processing; job envelope flat; PDF corpus; tenant-wide READY-only retrieval; grounded SourceRef; delete spans SQL+S3+Qdrant; capability map binds every shared AD; intended stack pins refreshed (SQLAlchemy 2.0.54, Alembic 1.20.0, openai 3.14.0, boto3 1.43.98, PyJWT 2.14.0, qdrant-client 1.13.3).
- (decision) AD-11 tenant corpus: all tenant members share documents; retrieval/chat only ready; assistant messages must carry SourceRef; empty retrieval does not unground. AD-12 delete/replace spans SQL, S3 key, and Qdrant points for that document_id.
- (event) spine finalized
- (decision by user) AD-10 envelope updated to match spec: product /api/v1 JSON is {data, error}; HTTP mapping 401/403/404/409; /health and /ready stay outside the envelope. Supersedes FastAPI detail-string body.
